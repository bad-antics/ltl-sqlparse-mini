// ltl-sqlparse-mini/main.ltl
//
// Mini SQL parser — SELECT / WHERE / JOIN / GROUP BY.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-sqlparse-mini v0.1.0")
    io::println("Mini SQL parser — SELECT / WHERE / JOIN / GROUP BY.")
    0
}
