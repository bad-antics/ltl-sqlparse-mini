// examples/demo.ltl — runnable demo for ltl_sqlparse_mini

import std::io
use ltl_sqlparse_mini as lib

fn main() {
    io::println("ltl_sqlparse_mini demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}